# Step 1: Use the specific Node version from your setup
FROM node:22.12.0

# Step 2: Set the working directory inside the container
WORKDIR /app

# Step 3: Copy package files first for faster builds (caching)
COPY package*.json ./

# Step 4: Install your backend dependencies (Express, Socket.io, Mongoose)
RUN npm install -g nodemon

# Step 5: Copy the rest of your backend source code
COPY . .

# Step 6: Expose the port your backend uses (usually 5000)
EXPOSE 5000

# Step 7: Command to start your server
# Use "npm start" or "node server.js" depending on your package.json
CMD ["npm", "run", "dev"]